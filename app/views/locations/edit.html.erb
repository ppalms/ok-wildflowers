<div class="w-full">
  <h1 class="font-bold text-4xl">Editing location</h1>
  <%= turbo_frame_tag @location do %>
    <%= render "form", location: @location %>
    <%= link_to "Cancel", location_path, class: "btn-secondary ml-2" %>
  <% end %>
</div>
