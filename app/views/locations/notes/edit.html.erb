<div class="w-full">
  <h1 class="font-bold text-4xl">Editing note</h1>
  <%= turbo_frame_tag @note do %>
    <%= render "form", note: @note %>
    <%= link_to "Cancel", note_path, class: "btn-secondary ml-2" %>
  <% end %>
</div>
