<%= form_with(model: location, class: "contents") do |form| %>
  <%= render "layouts/error_messages", errors: location.errors %>
  <div class="my-4">
    <%= form.label :name %>
    <%= form.text_field :name, class: "text-field", autofocus: true %>
  </div>
  <div class="inline">
    <%= form.submit class: "btn-primary" %>
  </div>
<% end %>
