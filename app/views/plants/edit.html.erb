<div id="edit_<%= dom_id @plant %>">
  <div class="mt-6">
    <h1>Editing plant</h1>
    <%= render "form", plant: @plant %>
    <%= link_to "Cancel", @plant, class: "btn-secondary" %>
  </div>
</div>
