<!DOCTYPE html>
<html>
  <head>
    <title>GardenHQ<%= " - " + content_for(:title) if content_for?(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1.0,user-scalable=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <nav>
      <ul class="flex justify-center bg-gray-100 py-5">
        <li class="m-2">
          <%= link_to "Home", root_path, class: "font-medium" %>
        </li>
        <li class="m-2">
          <%= link_to "Locations", locations_path, class: "font-medium" %>
        </li>
        <li class="m-2">
          <%= link_to "Plants", plants_path, class: "font-medium" %>
        </li>
      </ul>

      <% if user_signed_in? %>
    <div class="font-bold">
      Company name
    </div>
    <div>
      Current user name
    </div>
     <%= button_to "Sign out",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn-secondary" %>
  <% else %>
    <%= link_to "Sign in",
                new_user_session_path,
                class: "btn-primary" %>
  <% end %>
    </nav>
    <main class="container mx-auto mt-10 px-5 flex relative">
      <%= yield %>
    </main>
  </body>
</html>
